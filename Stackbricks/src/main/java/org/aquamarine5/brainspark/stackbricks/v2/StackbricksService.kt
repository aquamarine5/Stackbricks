package org.aquamarine5.brainspark.stackbricks.v2

import android.content.Context
import okhttp3.OkHttpClient

class StackbricksService(private val context: Context) {
    private val okHttpClient=OkHttpClient()
}